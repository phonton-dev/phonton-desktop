# v0.2.9

## Fixed

- **Native sidecar spawn:** Windows spawns vendor `phonton.exe serve` directly from Rust (`CREATE_NO_WINDOW`) — no `cmd /c` or `phonton.js` stdio:inherit chain.
- **Serve PATH parity:** Shell fallback always uses enriched PATH (same as verify).
- **Setup bootstrap:** CLI step calls `restartSidecar` + `waitForPing` directly — fixes stale React closure skipping sidecar start.
- **Diagnostics:** Separate `spawn failed` vs `ping timeout` errors; health check reuses existing serve on :47831.

## Added

- Tauri commands: `spawn_phonton_serve`, `stop_phonton_serve`
- `checkServeHealth()` and exported `waitForPing()` with 60s bootstrap window
