# Phonton Desktop

Private desktop shell for Phonton.

This package intentionally lives outside `phonton-dev/` so it does not ship in
the public `phonton-dev/phonton-cli` subtree. The desktop app is reserved for
commercial UI, team workflows, hosted indexing, billing, and other product
surfaces that are not part of the open CLI engine.

Current status: placeholder only.

